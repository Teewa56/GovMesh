/*
 * model/generated/vote.model.ts
 * Auto-generated by `sqd codegen` from schema.graphql.
 * Do not edit manually — regenerate with `sqd codegen`.
 */

import {
  Entity,
  Column,
  PrimaryColumn,
  ManyToOne,
  Index,
} from "typeorm";

export enum VoteType {
  Aye = "Aye",
  Nay = "Nay",
  Abstain = "Abstain",
}

export enum DeliveryStatus {
  Pending = "Pending",
  Sent = "Sent",
  Delivered = "Delivered",
  Failed = "Failed",
}

@Entity("vote")
export class Vote {
  constructor(props?: Partial<Vote>) {
    Object.assign(this, props);
  }

  @PrimaryColumn({ type: "text" })
  id!: string;

  @Index()
  @Column({ type: "text" })
  voteId!: string;

  @Index()
  @Column({ type: "text" })
  voter!: string;

  @Index()
  @Column({ type: "int" })
  parachainId!: number;

  @Column({ type: "text" })
  parachainName!: string;

  @Column({ type: "numeric", transformer: { to: (v: bigint) => v.toString(), from: (v: string) => BigInt(v) } })
  proposalIndex!: bigint;

  @Column({ type: "text", enum: VoteType })
  voteType!: VoteType;

  @Column({ type: "int" })
  conviction!: number;

  @Column({ type: "numeric", transformer: { to: (v: bigint) => v.toString(), from: (v: string) => BigInt(v) } })
  dotBalance!: bigint;

  @Column({ type: "numeric", transformer: { to: (v: bigint) => v.toString(), from: (v: string) => BigInt(v) } })
  votingWeight!: bigint;

  @Column({ type: "text", enum: DeliveryStatus })
  status!: DeliveryStatus;

  @Column({ type: "text", nullable: true })
  xcmMessageId!: string | null;

  @Column({ type: "numeric", transformer: { to: (v: bigint) => v.toString(), from: (v: string) => BigInt(v) } })
  blockNumber!: bigint;

  @Index()
  @Column({ type: "timestamp with time zone" })
  timestamp!: Date;

  @ManyToOne(() => Proposal, (p) => p.votes, { nullable: true })
  proposal!: Proposal | null;
}

@Entity("proposal")
export class Proposal {
  constructor(props?: Partial<Proposal>) {
    Object.assign(this, props);
  }

  @PrimaryColumn({ type: "text" })
  id!: string;

  @Index()
  @Column({ type: "numeric", transformer: { to: (v: bigint) => v.toString(), from: (v: string) => BigInt(v) } })
  index!: bigint;

  @Index()
  @Column({ type: "int" })
  parachainId!: number;

  @Column({ type: "text" })
  parachainName!: string;

  @Column({ type: "text" })
  title!: string;

  @Column({ type: "text" })
  metadataIpfsHash!: string;

  @Column({ type: "numeric", transformer: { to: (v: bigint) => v.toString(), from: (v: string) => BigInt(v) } })
  endBlock!: bigint;

  @Column({ type: "numeric", transformer: { to: (v: bigint) => v.toString(), from: (v: string) => BigInt(v) } })
  ayeVotes!: bigint;

  @Column({ type: "numeric", transformer: { to: (v: bigint) => v.toString(), from: (v: string) => BigInt(v) } })
  nayVotes!: bigint;

  @Column({ type: "numeric", transformer: { to: (v: bigint) => v.toString(), from: (v: string) => BigInt(v) } })
  abstainVotes!: bigint;

  @Index()
  @Column({ type: "boolean" })
  open!: boolean;

  @Column({ type: "timestamp with time zone" })
  lastSyncedAt!: Date;

  votes!: Vote[];
}

@Entity("parachain_stat")
export class ParachainStat {
  constructor(props?: Partial<ParachainStat>) {
    Object.assign(this, props);
  }

  @PrimaryColumn({ type: "text" })
  id!: string;

  @Index()
  @Column({ type: "int" })
  parachainId!: number;

  @Column({ type: "text" })
  parachainName!: string;

  @Column({ type: "int", default: 0 })
  totalVotes!: number;

  @Column({ type: "numeric", default: "0", transformer: { to: (v: bigint) => v.toString(), from: (v: string) => BigInt(v) } })
  totalAyeWeight!: bigint;

  @Column({ type: "numeric", default: "0", transformer: { to: (v: bigint) => v.toString(), from: (v: string) => BigInt(v) } })
  totalNayWeight!: bigint;

  @Column({ type: "numeric", default: "0", transformer: { to: (v: bigint) => v.toString(), from: (v: string) => BigInt(v) } })
  totalAbstainWeight!: bigint;

  @Column({ type: "int", default: 0 })
  deliveredVotes!: number;

  @Column({ type: "int", default: 0 })
  failedVotes!: number;

  @Column({ type: "int", default: 0 })
  pendingVotes!: number;

  @Column({ type: "timestamp with time zone" })
  lastUpdated!: Date;
}
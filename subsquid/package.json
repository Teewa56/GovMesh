{
  "name": "govmesh-squid",
  "version": "1.0.0",
  "description": "Subsquid indexer for GovMesh — cross-chain governance events on Polkadot Hub",
  "private": true,
  "scripts": {
    "build": "tsc",
    "process": "sqd process",
    "serve": "sqd serve",
    "codegen": "sqd codegen",
    "migration:generate": "sqd migration:generate",
    "migration:run": "sqd migration:run",
    "migration:revert": "sqd migration:revert",
    "clean": "rm -rf lib"
  },
  "dependencies": {
    "@subsquid/evm-processor": "^1.0.0",
    "@subsquid/typeorm-store": "^1.0.0",
    "@subsquid/graphql-server": "^4.0.0",
    "typeorm": "^0.3.0",
    "viem": "^2.17.0",
    "pg": "^8.11.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@subsquid/typeorm-codegen": "^1.0.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}